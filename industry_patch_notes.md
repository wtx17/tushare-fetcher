# 行业归档补丁：已审查案例与运行方法

`patch_industry_data.py` 是独立、离线的补丁，仅依赖现有 `pyarrow` 和 Python 标准库。
支持本机 macOS 与云端 Linux，Python ≥3.11。可以单独拷贝这个 `.py` 文件到云端运行。
不导入或修改 tushare-fetcher 核心模块，不调用 Tushare/ClickHouse，不需要 token。

## 本版具体修正

仅从 `index_member_all/data.parquet` 移除以下三条**完整匹配**的错误归属：

| 股票代码 | 股票名称 | in_date | 错误一级/二级/三级行业 |
| --- | --- | --- | --- |
| 000406.SZ | 石油大明(退市) | 19960628 | 传媒 / 数字媒体 / 视频媒体 |
| 000817.SZ | 辽河油田(退市) | 19980528 | 传媒 / 数字媒体 / 视频媒体 |
| 000956.SZ | 中原油气(退市) | 19991110 | 传媒 / 数字媒体 / 视频媒体 |

三条的代码路径均为 `801760.SI / 801767.SI / 857671.SI`，
`is_new=Y`、`out_date=NULL`。脚本会同时要求每只股票恰好存在一条应保留记录：
相同股票、名称、in_date、is_new 和空 out_date，行业路径为
`801960.SI / 801961.SI / 859611.SI`（石油石化 / 油气开采Ⅱ / 油气开采Ⅲ）。

如目标名称、退出日期、行业路径变化，保留记录缺失，错误行重复，或同一股票出现额外记录，
脚本拒绝应用，要求重新审查。如果仅剩完整匹配的保留行，识别为 `already_clean`；
如果归档不含该股票，识别为 `not_present`。不会按股票代码删掉全部记录。

真实副本及随后正式应用的修改均为 **7,538 → 7,535 行**，股票数量仍为 **5,906**，
保留其他记录的值、顺序和 Arrow schema/metadata。
不会人为设置退市日，不会改写保留记录的日期，也不会合并其他历史区间。

## 进一步调查的证据

### 1. 已下载申万附件中的历史归属

本地 `data/index_member_all/SwClass.rar` 内有 `StockClassifyUse_stock.xls`。
本次在临时目录解包并转换为只读分析副本，原附件未修改。
`Sheet1` 的三个股票记录分别是：

| 股票代码 | 计入日期 | 行业代码 | 更新日期 |
| --- | --- | --- | --- |
| 000406 | 1996-06-28 | 210101 | 2015-10-27 15:29:00 |
| 000817 | 1998-05-28 | 210101 | 2015-10-27 15:29:00 |
| 000956 | 1999-11-10 | 210101 | 2015-10-27 15:29:00 |

计入日期与归档中两套冲突行的 in_date 精确一致。该旧代码组还包括
中国石油 `601857`、中海油服 `601808` 等股票。
附件的 `SwClassCode_2021.xls` 使用另一套更新后的代码，列出
`720701` 为传媒/数字媒体/视频媒体，`750101` 为石油石化/油气开采Ⅱ/油气开采Ⅲ；
其中没有旧 `210101`。这些分类代码与 Tushare 的 `.SI` 指数代码属于不同编码列，
不能靠数字拼接或替换生成对应关系。

附件指纹（SHA-256）：

- `StockClassifyUse_stock.xls`：`15979d9cf8a3b83ccc8dadc967de52f35e667b4f4da5e4e4e3dd5a8bb1f17402`
- `SwClassCode_2021.xls`：`923492f4bcf3c7056904385a0769e4dda561904a29ecd9243f942680cef68c81`

附件还有一份截至某年 7 月末的最新分类 XLSX，未包含这三只股票，
因此不能用其缺席来推断应删除全部历史归属。

### 2. 发行人托管的业务证据

中国石油官网收录的中金公司《辽河金马油田股份有限公司独立财务顾问报告》
将石油大明、中原油气列为与辽河油田主营业务可比的石油勘探开采企业，
并在比较表中列出 `000406`、`000956`、`000817`。

[原始报告](https://www.petrochina.com.cn/petrochina/gsgg/201403/e66ded96373f440c958a9fe41b41d810/files/083b299c9c3f47bfa5cd6f3ca702eb29.pdf)

本次通过网页搜索索引核对了这部分文本；直接下载 PDF 返回 HTTP 403，
未取得全文副本。该报告用于核验公司业务，不声称它定义了申万 2021 行业代码。

### 3. 当前归档的交叉证据与判断边界

- 申万归档自身已有三条同日起点的完整石油石化记录，补丁保留这些源记录。
- 中信归档也将三只股票归入石油石化；仅作旁证，不将中信代码映射成申万代码。
- 申万归档没有全字段完全相同的多余行，所以不是普通分页重复造成的重复观测。
- 本地 manifest 指向第三方 API 代理；目前环境没有现成的 Tushare token，
  本次未重拉代理或官方接口，也未在文件中写入用户此前提供的凭据。

**审查结论**：上述三条“视频媒体”归属与历史分类记录、业务资料及归档已有油气记录不符，
可作为三个明确案例排除。历史分类转换或上游拼接是值得追查的环节，
但尚无中间处理记录证明具体污染步骤；补丁没有把这个推测推广为通用清洗规则。

## 本版保留的待审查问题

| 类别 | 已查到的特征 | 本版处理 |
| --- | --- | --- |
| 申万另外 62 只多当前记录 | 59 只最新 in_date=20260701，3 只=20260821；起点不同，旧行仍为空退出日期 | 原样保留，完整行写入报告 |
| 中信 39 条 l3_code 缺失 | 全部为 N 的历史记录，属于水泥Ⅲ/玻璃Ⅲ，结束日期不晚于 20191201 | 原样保留，缺失行写入报告 |

前者不产生本次同优先级异常，但仍需核验旧区间终点；后者集中在旧行业记录，
不能未经版本核验用现行行业字典补码。两者都不因本版已通过而被认定为数据无误。

补丁还会按归档日期范围审计有效区间：选取最高 `(in_date, is_new)`，
检查并列记录的所有输出字段是否一致。发现未知冲突会阻止整个应用。
检查覆盖日历日而非只覆盖交易日，因此对仅发生在休市日的冲突也会保守报告。

## 使用方法

先保存只读预览报告。`--data-dir` 接受包含 `index_member_all` 的数据根目录；
省略时使用脚本所在目录下的 `data`。

```bash
python patch_industry_data.py --data-dir /path/to/tushare-fetcher/data \
  --report /path/to/industry_review.json
```

`--dry-run` 可显式添加，也可省略。报告文件必须是数据目录外的新文件，防止覆盖已有记录。
预览模式不会创建锁、备份或修改数据；仅显式 `--report` 写出报告。
如果输出 `BLOCKED`，退出码为 2，先检查报告，不会进行部分清洗。

应用补丁：

```bash
python patch_industry_data.py --data-dir /path/to/tushare-fetcher/data --apply
```

应用前暂停该目录的下载任务和读取进程。脚本的文件锁仅协调本补丁进程，
原抓取器不使用这把锁。程序会先校验输入与生成候选，保存完整备份，再替换文件。
默认备份父目录为数据目录同级的 `industry_patch_backups`，也可用
`--backup-dir /path/to/backups` 指定，必须位于数据目录外。

备份子目录包括：

- `original_data.parquet`、`original__manifest.json`：原始文件，字节级保留。
- `patched_data.parquet`、`patched__manifest.json`：生成的候选文件。
- `patch_log.json`：完整修正行、保留行、输入输出指纹、待审查问题和操作状态。

脚本更新 `partitions.all.rows/bytes/sha256` 与 manifest `updated_at`；
保留原始 `fetched_at`、`raw_rows`、分页/请求信息、归档范围和 schema 信息。
这些抓取指标描述原下载过程，不改写为清洗后的统计。
成功后重新读取归档验证一致性；再次应用返回 `no_change`，不会新建备份或重写文件。

如果要恢复，使用运行时实际打印的备份路径，保留同一 `--data-dir`：

```bash
python patch_industry_data.py --data-dir /path/to/tushare-fetcher/data \
  --restore /path/to/industry_patch_backups/sw_oil_video_media_20260909_v1_xxxxxxxx
```

每个文件通过同目录临时文件和原子替换安装，但两个文件的替换整体不具备原子性。
中途故障时 `patch_log.json` 可能停留在 `prepared`；可用同一恢复命令恢复原文件。
恢复前校验备份内容，并要求现有文件分别匹配本次操作前或后的指纹。
若已经被其他下载/编辑修改，会拒绝恢复，防止覆盖后续数据。

以后抓取器重新下载 `all` 分区可能再次带回相同污染。需要时独立重新运行本补丁；
未向抓取器增加自动钩子，目标记录发生变化时仍须重新审查。

## 如何添加下一个案例

在独立补丁中添加单独审查过的完整行条件及明确动作，附来源和理由，并加入对应测试。
本版 `REVIEWED_CASES` 专门描述同一类“错误视频媒体行与既有油气行并存”的三个案例。
不同成因应增加自己的处理函数，不能为了让数据通过而放宽现有匹配条件。

## 初次交付验证（正式应用前）

- 新补丁 25 项测试、原抓取器 22 项测试，共 **47 项通过**。
- 默认预览、未知冲突拒绝、完整记录漂移拒绝、schema/manifest 验证、重复应用、
  文件权限保持、并发补丁锁、输入中途变化拒绝均已测试。
- 模拟 Parquet 已写但 manifest 写入失败后，恢复操作可还原两份原文件。
- 已更新数据或损坏备份不会被恢复操作覆盖。
- 在真实归档临时副本上执行应用、fetcher `verify`、quant_data 区间展开及恢复。
- 原始归档和五个核心源文件保持不变；本次没有对原下载目录执行 `--apply`。

本机复验命令：

```bash
/opt/anaconda3/envs/quant/bin/python -m pytest \
  tests/test_patch_industry_data.py tests/test_sync_tushare.py -q
```

原目录的预览为 `patch_reports/20260909/review.json`，
真实副本验收记录为 `patch_reports/20260909/validation.json`。
验收未连接云端 Notebook 或真实 ClickHouse 日历，不代表整条远端研究链路已经运行。

## 2026-09-09 正式本地应用与真实 Loader 结果

用户随后明确要求在本地处理原归档并运行行业 Loader，现已完成正式应用。
原申万归档已清洗为 **7,535 行**，备份位于
`industry_patch_backups/sw_oil_video_media_20260909_v1_72gu4082`。
中信归档和抓取器核心代码未修改；备份、实际归档与 manifest 指纹已核验。

清洗前真实 Loader 已复现原 SchemaMismatchError；清洗后使用真实 ClickHouse 交易日历，
对 2023-07-01 至 2024-03-11 和三大指数历史并集完整运行，成功返回：

```text
IndustryPanel(shape=(168, 3611), n_industries=31, coverage=0.9778)
```

一级行业加载耗时 **237.250 秒**。三只修正股票全区间的 l1/l2/l3 分别为
`801960.SI / 801961.SI / 859611.SI`，均已实际调用 Loader 复核。
按当日实际指数成分计算，行业覆盖率为 **99.9441%**（302,231 / 302,400）。
剩余 169 个缺失位置：`300114.SZ` 在归档中完全无记录，缺 168 日；
`603456.SH` 新区间从 2023-07-04 开始、旧区间已于 2021-07-29 结束，缺 2023-07-03 一日。
本补丁不推测填补这些历史缺口。

此验证实际运行了本地行业加载环节，没有重新计算分钟价格、前向收益或执行云端 Notebook。
完整指标与缺失明细在 `patch_reports/20260909/loader_after_patch.json`，
解码后的面板在同目录 `industry_l1_after_patch.parquet`，
执行计划和完成记录在 `local_run_plan.md`，正式应用审计在 `apply_local.json`。
